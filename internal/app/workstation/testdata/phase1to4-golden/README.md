# Phase 1--4 Go PNG baselines

These lossless images are the canonical Go visual targets for the initial
Python shell port. `manifest.json` owns their reproducibility inputs, SHA-256
values, and pixel tolerances. The similarly scoped JPEGs under
`python_migration/screenshots/` are inspection aids only.
