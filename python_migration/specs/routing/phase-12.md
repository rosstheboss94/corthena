# Phase 12 Task Route

Non-authoritative navigation aid; canonical behavior remains in linked specs.

- Required: `AGENTS.md`, Phase 12 in `roadmap.md`, `python-migration.md`,
  `migration-baseline.md`, `technology-stack.md`, `frontend/foundation.md`,
  `quality.md`, and `design-pattern.md`.
- Conditional: `quality-visualization.md` for capture/comparison work and
  `api.md` only when a public/process boundary is deliberately introduced.
- Scope: create the reproducible Python scaffold and initial Phase 1--4
  workstation shell port, including entry point, locked UI thread, bundled
  assets, hidden smoke capture, deterministic replay, layout/preferences, and
  PNG parity evidence.
- Before `pyproject.toml` exists, do not invent Python verification commands.
  Once it exists, record the exact Phase 12 commands there or in this route.
- Exclude: coordinator, worker, data/import, experiment, job/result, model,
  inference, and real-backend ports.
