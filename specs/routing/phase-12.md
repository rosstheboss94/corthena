# Phase 12 Task Route

Non-authoritative navigation aid; canonical behavior remains in linked specs.

- Required: `AGENTS.md`, Phase 12 in `roadmap.md`, `python-migration.md`,
  `migration-baseline.md`, `technology-stack.md`, `ui/foundation.md`,
  `quality.md`, and `design-pattern.md`.
- Conditional: `quality-visualization.md` for capture/comparison work and
  `api.md` only when a public/process boundary is deliberately introduced.
- Accepted scope: the reproducible root Python scaffold and initial Phase 1--4
  workstation shell, including entry point, locked UI thread, bundled assets,
  hidden smoke capture, deterministic replay, layout/preferences, and retained
  PNG evidence.
- `pyproject.toml` and `README.md` own the implemented verification commands.
- Historical exclusion: coordinator, worker, data/import, experiment, job/result, model,
  inference, and real-backend ports.
